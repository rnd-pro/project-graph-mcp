--- src/validate-pipeline.js ---
@sig 4d7aa91a
estimateTokens(text)→Math.ceil|estimate token count from char length (÷4)
walkJSFiles(dir)→readdirSync,statSync,SKIP_DIRS.has|walk dir for .js/.mjs files, skip vendor/node_modules
export validatePipeline(projectPath,options=)→validateCtxContracts,decompileProject,walkJSFiles,parse,readFileSync|run full Mode 4 validation pipeline|returns PASS/FAIL with contract, AST, token reports
PATTERNS: 4-step pipeline (contracts→decompile→AST verify→tokens)|single-call validation
EDGE_CASES: missing .full/→skip AST verify|missing src/→skip tokens|strict mode for extra contract checks
