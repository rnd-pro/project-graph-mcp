--- src/ai-context.js ---
@sig 4de57d15
export getAiContext(dirPath,options=)→resolve,getSkeleton,estimateTokens,getGraph,getProjectDocs,findJSFiles|get project context including skeleton, docs, and optionally compressed files
PATTERNS: token estimation heuristic|parallel generation
EDGE_CASES: gracefully skips unreadable files|handles missing files