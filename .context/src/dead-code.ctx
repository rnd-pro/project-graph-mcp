--- src/dead-code.js ---
@sig c8e68f61
findProjectRoot()→resolve,dirname,join,existsSync|walks up dir tree to find package.json
analyzeFileLocals()→parse,varDecls.push,declSites.add,importDecls.push,walk.simple,topLevelExportedNames.add|parses AST to find unused local vars and imports
export getDeadCode()→resolve,findJSFiles,findProjectRoot,readFileSync,relative,analyzeFile|scans dir for unused fns, classes, exports, vars, imports
PATTERNS: AST traversal|cross-file dep tracking|heuristic usage regex
EDGE_CASES: syntax err→skip file|test/css/tpl files→ignored
