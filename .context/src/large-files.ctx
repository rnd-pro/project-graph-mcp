--- src/large-files.js ---
@sig 536d260c
@enrich: Replace each {DESCRIBE} below. Read src/large-files.js for context.
  Rules: max 80ch, pipe|separated, abbrev (fn/ret/cfg/init/auth/db/msg).
  Save this file after filling all markers. Remove @enrich lines when done.
analyzeFile(filePath,rootDir)→readFileSync,relative,code.split,parse,walk.simple,reasons.push|{DESCRIBE}
export getLargeFiles(dir,options=)→resolve,findJSFiles,analyzeFile,files.map,items.filter,items.sort|{DESCRIBE}
PATTERNS: {DESCRIBE}
EDGE_CASES: {DESCRIBE}
