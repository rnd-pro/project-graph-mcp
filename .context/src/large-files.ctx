--- src/large-files.js ---
@sig 536d260c
analyzeFile(filePath:string,rootDir:string)→readFileSync,relative,code.split,parse,walk.simple,reasons.push|parse file, count structures, and assign size rating
export getLargeFiles(dir:string,options:Object=)→resolve,findJSFiles,analyzeFile,files.map,items.filter,items.sort|analyze directory to find overly large and complex files
PATTERNS: AST visitor via Acorn|threshold-based scoring
EDGE_CASES: ignores parse errors gracefully