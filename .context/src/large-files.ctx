--- src/large-files.js ---
@sig 536d260c
analyzeFile(filePath,rootDir)→readFileSync,relative,code.split,parse,walk.simple,reasons.push|parse file, count structures, and assign size rating
export getLargeFiles(dir,options=)→resolve,findJSFiles,analyzeFile,files.map,items.filter,items.sort|analyze directory to find overly large and complex files
PATTERNS: AST visitor via Acorn|threshold-based scoring
EDGE_CASES: ignores parse errors gracefully