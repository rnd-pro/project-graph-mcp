--- src/large-files.js ---
@sig 49072ccd
@enrich: Replace each {DESCRIBE} below. Read src/large-files.js for context.
  Rules: max 80ch, pipe|separated, abbrev (fn/ret/cfg/init/auth/db/msg).
  Save this file after filling all markers. Remove @enrich lines when done.
analyzeFile(filePath:string,rootDir:string)→LargeFileItem→readFileSync,relative,code.split,parse,walk.simple,reasons.push|{DESCRIBE}
export getLargeFiles(dir:string,options:Object=)→Promise<{total: number, stats: Object, items: LargeFileItem[]→resolve,findJSFiles,analyzeFile,files.map,items.filter,items.sort|{DESCRIBE}
PATTERNS: {DESCRIBE}
EDGE_CASES: {DESCRIBE}
