--- src/compact/validate-pipeline.js ---
@sig 562a544c
@enrich: Replace each (auto-documented) below. Read src/compact/validate-pipeline.js for context.
  Rules: max 80ch, pipe|separated, abbrev (fn/ret/cfg/init/auth/db/msg).
  Save this file after filling all markers. Remove @enrich lines when done.
$(e,s)→c,t,f.split,u.startsWith,r.push,l|(auto-documented)
export validatePipeline(e,s=)→Date.now,p,u,i,n,w|run full Mode 4 validation pipeline|returns PASS/FAIL with contract, AST, token reports
PATTERNS: 4-step pipeline (contracts→decompile→AST verify→tokens)|single-call validation
EDGE_CASES: missing .full/→skip AST verify|missing src/→skip tokens|strict mode for extra contract checks
v(a)|estimateTokens — internal helper
S(a,b)|checkCompactStyle — internal helper
E(e,s)|exported as fixCompactStyle:auto-fix compact style: generate .ctx + terser + split
