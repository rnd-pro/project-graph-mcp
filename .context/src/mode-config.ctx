--- src/mode-config.js ---
@sig 59f3a19d
@vars r=CONFIG_FILE,n=DEFAULTS
export getConfig(projectDir)→join,existsSync,readFileSync,JSON.parse|read .context/config.json, merge with defaults
export setConfig(projectDir,config)→join,dirname,existsSync,mkdirSync,getConfig,writeFileSync|save mode config|validates mode 1-4
export getModeDescription(mode)|return human-readable desc for mode 1-4
export getModeWorkflow(mode)|return read/edit/docs/validate workflow per mode
PATTERNS: config merge with DEFAULTS|.context/config.json storage
EDGE_CASES: missing config→defaults|invalid mode→throw error|missing dir→mkdirSync
