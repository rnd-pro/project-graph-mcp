--- src/similar-functions.js ---
@sig 36a3e214
extractParamName(param)|extract parameter name string from AST node
extractSignatures(filePath,rootDir)→readFileSync,relative,parse,buildSignature,signatures.push,name.startsWith|traverse AST to build detailed structural signatures for file
buildSignature(node,name,file)→extractParamName,params.map,calls.push,walk.simple,hashBodyStructure|construct signature object with structure hash and calls
hashBodyStructure(body)→structure.push,walk.simple,structure.join|traverse function body to build structural flow sequence string
calculateSimilarity(a,b)→reasons.push,paramNames.includes,paramNames.filter,Math.max,Math.round,commonParams.join|compare two signatures to calculate similarity percentage score
export getSimilarFunctions(dir,options=)→resolve,findJSFiles,extractSignatures,allSignatures.push,calculateSimilarity,pairs.push|extract and compare signatures across directory for similar pairs
PATTERNS: structural hashing of control flow|pair-wise comparison scoring
EDGE_CASES: skips very small functions|ignores overloads (same name/file)