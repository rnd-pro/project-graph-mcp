--- src/db-analysis.js ---
@sig 6cd06c23
@enrich: Replace each {DESCRIBE} below. Read src/db-analysis.js for context.
  Rules: max 80ch, pipe|separated, abbrev (fn/ret/cfg/init/auth/db/msg).
  Save this file after filling all markers. Remove @enrich lines when done.
export getDBSchema(dir:string)→Promise<Object>→parseProject,tables.map,tables.reduce|{DESCRIBE}
export getTableUsage(dir:string,tableName:string)→Promise<Object>→parseProject,buildGraph,readers.some,readers.push,writers.some,writers.push|{DESCRIBE}
export getDBDeadTables(dir:string)→Promise<Object>→parseProject,buildGraph,referencedTables.add,referencedTables.has,schemaTables.filter,collectReferencedColumns|{DESCRIBE}
collectReferencedColumns(parsed:Object)→Set<string>→columns.add|{DESCRIBE}
PATTERNS: {DESCRIBE}
EDGE_CASES: {DESCRIBE}
