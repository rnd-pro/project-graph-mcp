--- src/type-checker.js ---
@sig 8ab664fb
@enrich: Replace each {DESCRIBE} below. Read src/type-checker.js for context.
  Rules: max 80ch, pipe|separated, abbrev (fn/ret/cfg/init/auth/db/msg).
  Save this file after filling all markers. Remove @enrich lines when done.
detectTsc()→execSync|null, path: string|null →execSync|null, path: string|null →execSync|null, path: string|null →execSync|{DESCRIBE}
parseDiagnosticLine(line,baseDir)→line.match,parseInt|null→line.match,parseInt|null→line.match,parseInt|null→line.match,parseInt|{DESCRIBE}
buildArgs(dir,options=)→join,existsSync,args.push|{DESCRIBE}
export checkTypes(dir,options=)→resolve,detectTsc,buildArgs,path.includes,spawn,stdout.on|null, diagnostics: TypeDiagnostic[], summary: Object, hint: string|null →resolve,detectTsc,buildArgs,path.includes,spawn,stdout.on|null, diagnostics: TypeDiagnostic[], summary: Object, hint: string|null →resolve,detectTsc,buildArgs,path.includes,spawn,stdout.on|null, diagnostics: TypeDiagnostic[], summary: Object, hint: string|null →resolve,detectTsc,buildArgs,path.includes,spawn,stdout.on|{DESCRIBE}
PATTERNS: {DESCRIBE}
EDGE_CASES: {DESCRIBE}
