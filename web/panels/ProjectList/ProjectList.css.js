export default /*css*/ `
:host {
  display: block;
  height: 100%;
  overflow-y: auto;
  padding: 16px;
  background: var(--sn-bg-primary);
  color: var(--sn-fg-primary);
}
.empty {
  color: var(--sn-fg-muted);
  padding: 24px;
  text-align: center;
}
`;
