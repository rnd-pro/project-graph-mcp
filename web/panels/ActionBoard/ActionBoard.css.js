export default /*css*/ `
:host {
  display: flex;
  flex-direction: column;
  height: 100%;
  background: var(--sn-bg-primary);
  color: var(--sn-fg-primary);
  overflow: hidden;
}
.list {
  flex: 1;
  overflow-y: auto;
  padding: 16px;
}
`;
