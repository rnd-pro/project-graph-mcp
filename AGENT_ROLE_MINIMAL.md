# Project Graph MCP

You have **Project Graph MCP** tools available.

## Quick Start
1. Run `get_agent_instructions` to get project guidelines
2. Run `get_structure` to see project layout
3. Run `get_full_analysis` for code quality report

## Available Tools
Use `get_custom_rules` to see framework-specific rules (auto-detected).
